import styled from 'styled-components'

export const StandartForm = styled.form`
    padding: 20px;
    border: 1px solid #e9ecef;
    position: relative;
    top: -1px;
`

export const StandartInputContainer = styled.form`
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-bottom: 20px;
    position: relative;
`